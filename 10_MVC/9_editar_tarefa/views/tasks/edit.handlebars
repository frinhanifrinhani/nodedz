<h1>Editando a task {{task.title}}</h1>
<form action="/tasks/add" method="post">
    <input type="hidden" name="id" name="{{task.id}}">
    <div class="form-control">
        <label for="title">Titulo:</label>
        <input type="text" name="title" placeholder="O que você vai fazer?" value="{{task.title}}">
    </div>
    <div class="form-control">
        <label for="description">Descição:</label>
        <textarea type="text" name="description" placeholder="Descreva a tarefa...">{{task.description}}
        </textarea>
    </div>
    <input type="submit" value="Editar">
</form>